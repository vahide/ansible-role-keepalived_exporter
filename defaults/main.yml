---

keepalived_exporter_version: 0.5.0
keepalived_exporter_binary_local_dir: ""
keepalived_exporter_web_listen_address: "0.0.0.0:9650"

keepalived_exporter_textfile_dir: "/var/lib/keepalived_exporter"

keepalived_exporter_tls_server_config: {}

keepalived_exporter_http_server_config: {}

keepalived_exporter_basic_auth_users: {}

keepalived_exporter_enabled_collectors:
# - systemd
  - textfile:
      directory: "{{ keepalived_exporter_textfile_dir }}"
#  - filesystem:
#      ignored-mount-points: "^/(sys|proc|dev)($|/)"
#      ignored-fs-types: "^(sys|proc|auto)fs$"

keepalived_exporter_disabled_collectors: []










































































































































































































































